---
import siteData from "../data/siteData.json";

const {
  title,
  description,
  url = Astro.url,
  image,
  frontmatter,
  robots,
} = Astro.props;
---

<!-- SEO -->
<link rel="canonical" href={url} />
<title>{title ? title:siteData.title}</title>
<meta name="description" content={description ? description:siteData.description} />

<!-- Open Graph -->
<meta property="og:site_name" content={siteData.site_name} />
<meta property="og:title" content={title ? title:siteData.title} />
<meta property="og:description" content={description ? description:siteData.description} />
<meta property="og:url" content={url} />
<meta property="og:image" content={image?.url || siteData.image.src} />
<meta property="og:image:url" content={image?.url || siteData.image.src} />
<meta
  property="og:image:secure_url"
  content={image?.url || siteData.image.src}
/>
<meta property="og:image:type" content={image?.mime || siteData.image.mime} />
<meta property="og:image:width" content={image?.width || siteData.image.width} />
<meta property="og:image:height" content={image?.height || siteData.image.height} />
<meta property="og:image:alt" content={image?.alt || siteData.image.alt} />

<!-- Twitter -->
<meta name="twitter:title" content={title ? title:siteData.title} />
<meta name="twitter:description" content={description ? description:siteData.description} />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content={image?.url || siteData.image.src} />
<meta name="twitter:image:alt" content={title ? title:siteData.title} />
<meta name="twitter:domain" content={import.meta.env.SITE} />

{robots && <meta name="robots" content="noindex, nofollow" />}
