---
import ClientLogos from "./ClientLogos.astro";
import ContactForm from "../ContactForm.astro";
const {
    heading,
    subheading,
    showClientLogos,
    formId,
    formTitle,
    formDescription,
    formButtonLabel,
} = Astro.props;
---
<div id="cta" class="relative overflow-hidden bg-gray-800">
    <div class="hidden sm:absolute sm:inset-0 sm:block" aria-hidden="true">
      <svg class="absolute bottom-0 right-0 mb-48 translate-x-1/2 transform text-gray-700 lg:top-0 lg:mb-0 lg:mt-28 xl:translate-x-0 xl:transform-none" width="364" height="384" viewBox="0 0 364 384" fill="none">
        <defs>
          <pattern id="eab71dd9-9d7a-47bd-8044-256344ee00d0" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">
            <rect x="0" y="0" width="4" height="4" fill="currentColor" />
          </pattern>
        </defs>
        <rect width="364" height="384" fill="url(#eab71dd9-9d7a-47bd-8044-256344ee00d0)" />
      </svg>
    </div>
    <div class="relative pb-16 pt-6 sm:pb-24">
      <main class="mt-16 sm:mt-24">
        <div class="mx-auto max-w-7xl">
          <div class="lg:grid lg:grid-cols-12 lg:gap-8">
            <div class="px-6 sm:text-center md:mx-auto md:max-w-2xl lg:col-span-6 lg:flex lg:items-center lg:text-left">
              <div>
                <h1 class="text-4xl font-bold tracking-tight text-white sm:text-5xl md:text-6xl">{heading}</h1>
                <p class="mt-3 text-base text-gray-300 sm:mt-5 sm:text-xl lg:text-lg xl:text-xl">{subheading}</p>
                <!-- Client Logos -->
                {showClientLogos && <ClientLogos/>}
              </div>
            </div>
            <div class="mt-16 sm:mt-24 lg:col-span-6 lg:mt-0">
              <ContactForm formId={formId} buttonLabel={formButtonLabel} title={formTitle} description={formDescription} bgColor="bg-white"/>
            </div>
          </div>
        </div>
      </main>
    </div>
</div>